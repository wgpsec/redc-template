# Docker Installation

This is a userdata template for installing Docker Engine.

## Features

1. Download and execute Docker installation script
2. Start and enable Docker service
3. Add current user to docker group
