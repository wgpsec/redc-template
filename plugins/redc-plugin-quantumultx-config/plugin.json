{
  "name": "redc-plugin-quantumultx-config",
  "version": "1.0.0",
  "description": "场景部署后自动生成 QuantumultX 订阅文件 (Base64 编码 ss:// URI)",
  "description_en": "Auto-generate QuantumultX subscription file (Base64-encoded ss:// URIs) after deployment",
  "author": "wgpsec",
  "homepage": "https://github.com/wgpsec/redc-template",
  "category": "proxy",
  "tags": ["quantumultx", "proxy", "shadowsocks", "ios"],
  "min_redc_version": "3.1.0",
  "capabilities": {
    "hooks": {
      "post-apply": "hooks/post-apply.sh"
    }
  },
  "config_schema": {
    "port": {
      "type": "string",
      "required": false,
      "description": "Shadowsocks 端口 (默认从 terraform.tfvars 读取 port)",
      "default": ""
    },
    "password": {
      "type": "string",
      "required": false,
      "description": "Shadowsocks 密码 (默认从 terraform.tfvars 读取 password)",
      "default": ""
    },
    "filename": {
      "type": "string",
      "required": false,
      "description": "输出文件名",
      "default": "quantumultx.txt"
    },
    "tag_prefix": {
      "type": "string",
      "required": false,
      "description": "节点名称前缀",
      "default": "proxy"
    }
  }
}
